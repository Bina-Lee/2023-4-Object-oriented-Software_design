#if 0
#include<iostream>
#include<vector>
using namespace std;
class sample{
    vector<{type}> {vector_name}{{size}};
    {vector}.push_back(data);
    {vector}.pop_back();
    if({vector}.empty());
    cout<<{vector}.back<{vector}.front<<endl;

    auto p={vector}.begin;
    cout<< *p <<endl;
    p++;

    v.erase({adsf});
};
#endif

#if 01
#include<iostream>
#include<algorithm>
#include<vector>
#include<string>
bool compare(int& a,int& b){
    return a<b;
}

int main(){
    vector<int>list{1,2,3,4};
    sort(list.begin(),list.end(),compare);
}
#endif